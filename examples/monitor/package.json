{
  "name": "@latkit/monitor-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @latkit/colormaps build && pnpm --filter @latkit/gpu build && pnpm --filter @latkit/monitor build",
    "dev": "vite --host 127.0.0.1 --port 5190",
    "prebuild": "pnpm --filter @latkit/colormaps build && pnpm --filter @latkit/gpu build && pnpm --filter @latkit/monitor build",
    "build": "pnpm run typecheck && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview --host 127.0.0.1 --port 5190"
  },
  "dependencies": {
    "@latkit/colormaps": "workspace:*",
    "@latkit/gpu": "workspace:*",
    "@latkit/monitor": "workspace:*"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.71",
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
