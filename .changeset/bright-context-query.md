---
'@latkit/network': minor
'@latkit/embed': minor
---

Add threshold-gated `contextmenu` events, synchronous CPU `hitTest` and `locate` queries, subset fitting, and view-preserving item reveal without changing the existing picker hot path. Forward subset fitting and reveal through `NetworkElement`.
