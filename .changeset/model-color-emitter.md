---
'@latkit/model': minor
---

- Added: `RGBA` and `Colormap` types, `validateRgba`, `bakeColormap`, and `createEmitter` with its `Emitter` type; the one home for the color vocabulary and the event dispatcher every renderer shares.
