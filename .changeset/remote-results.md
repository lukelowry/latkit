---
'@latkit/remote': minor
---

Add `serveResults` and `connectResults`: a `Results` served across a port, one class's batches per stream, with an optional `maxSignals` bound on what one read may select.
