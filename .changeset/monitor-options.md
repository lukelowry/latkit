---
'@latkit/monitor': minor
---

Add `timeRange` (a window over the series' time axis; null shows the whole span), `focusColor` (null brightens the selected trace's own color), and `unselectedAlpha` (the alpha of every other trace while an element is selected). Readings map the cursor through the window.
