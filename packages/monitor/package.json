{
  "name": "@latkit/monitor",
  "version": "0.2.0",
  "description": "WebGPU trace monitor for Latkit.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lukelowry/latkit.git",
    "directory": "packages/monitor"
  },
  "license": "MIT",
  "sideEffects": false,
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project @latkit/monitor",
    "bench": "vitest bench --run --project @latkit/monitor"
  },
  "dependencies": {
    "@latkit/gpu": "workspace:*",
    "@latkit/model": "workspace:*",
    "@webgpu/types": "^0.1.71"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
