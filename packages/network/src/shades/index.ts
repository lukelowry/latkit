/**
 * `@latkit/network/shades` — shades built on the public hook, so a page gets a finished effect
 * with one call and a proof that the hook is enough to write its own.
 *
 * @packageDocumentation
 */

export { spotlight } from './spotlight.js';
export type { SpotlightOptions } from './spotlight.js';
