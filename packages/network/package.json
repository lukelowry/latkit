{
  "name": "@latkit/network",
  "version": "0.0.0",
  "description": "WebGPU network renderer for Latkit.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lukelowry/latkit.git",
    "directory": "packages/network"
  },
  "license": "MIT",
  "sideEffects": false,
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project @latkit/network",
    "bench": "vitest bench --run --project @latkit/network"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "jsdom": "^26.1.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
